class Shapes {
    constructor(color, textColor, shapeColor) {
        this.color = color;
        this.textColor = textColor;
        this.shapeColor = shapeColor;
    }
}

class Rectangle extends Shapes {
    constructor(color, textColor, shapeColor, )
}